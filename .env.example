# Database (optional - defaults to file:./data/local.db for local development)
# For Turso: DATABASE_URL=libsql://your-database.turso.io?authToken=your-token

# Vercel Environment (auto-set by Vercel - do not set manually)
# VERCEL_ENV=production|preview|development
# VERCEL_URL=your-deployment-url.vercel.app (without https://)

# Better Auth
# BETTER_AUTH_SECRET=your-secret-key-here (required in production/preview, min 32 chars)
# BETTER_AUTH_URL=https://your-domain.com (optional in preview - auto-derived from VERCEL_URL)
# BETTER_AUTH_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com (comma-separated)

# Vercel Cron (required in production/preview - for session cleanup job)
# CRON_SECRET=your-cron-secret-here

# Upstash Redis (required in production - for rate limiting)
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here
